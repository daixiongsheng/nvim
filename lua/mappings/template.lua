local M = {}
--- @param Map Map
M.setup = function(Map)
  -- local status, hop = pcall(require, "hop")
  -- if not status then
  --   vim.notify("没有找到 hop")
  --   return {}
  -- end
  return {}
end
return M
